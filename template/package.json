{
  "name": "my-lib",
  "version": "1.0.0",
  "main": "public/index.html",
  "scripts": {
    "copy": "rm -rf dist && mkdir dist && cp ../assets/pdf-open-parameters.pdf dist",
    "start": "npm run copy && parcel",
    "build": "npm run copy && parcel build"
  },
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "parcel": "^2.2.0"
  }
}
